{
    "scripts": {
        "postinstall": "sh install.sh",
        "start": "concurrently --no-color 'npm:start-*'",
        "start-jet": "cd EmployeeManager;ojet serve",
        "start-mock": "cd mock-server;npm start"
    },
    "devDependencies": {
        "concurrently": "^4.0.1"
    }
}
